name: Update Resume
run-name: Update resume 🚀
on: 
    push:
        branches:
            - main

jobs:
  update_resume:
    runs-on: ubuntu-latest
    steps:
        - run: sudo apt-get install poppler-utils
        - run: python createImage.py
        - run: git config --global user.email "sondzvl129@gmail.com"
        - run: git config --global user.name "sonnguyenHT"
        - run: git add .
        - run: git commit -m "Update resume"
        - run: git push origin main

      
